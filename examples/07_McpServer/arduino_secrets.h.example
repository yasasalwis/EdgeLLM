// Copy to `arduino_secrets.h` (gitignored) and fill in.
#pragma once

#define WIFI_SSID "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// Optional: require this bearer token on every MCP request. Leave empty to run
// open on your trusted LAN. A long random value is recommended if set.
#define MCP_BEARER_TOKEN ""
